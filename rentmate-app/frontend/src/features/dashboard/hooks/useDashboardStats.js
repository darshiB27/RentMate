// Custom Analytics stats lookup hook
// Purpose: Queries analytics aggregation endpoints for owners dashboard panels.
export const useDashboardStats = () => {
  return { data: null, isLoading: false };
};
