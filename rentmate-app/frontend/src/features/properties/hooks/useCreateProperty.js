// Custom Listing Creation hook
// Purpose: Posts form details, uploads images, and invalidates property lists caches.
export const useCreateProperty = () => {
  return { mutate: () => {}, isLoading: false };
};
