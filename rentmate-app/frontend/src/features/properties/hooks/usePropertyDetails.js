// Custom Property Details hook
// Purpose: Fetches specific listing detail information matching parameter keys.
export const usePropertyDetails = (id) => {
  return { data: null, isLoading: false };
};
